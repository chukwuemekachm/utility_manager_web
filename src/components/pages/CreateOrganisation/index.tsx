import * as React from 'react';
import styled from '@emotion/styled';

export default function CreateOrganisation(): React.ReactElement<{}> {
  return <CreateOrganisation.Wrapper>Create New Organisation will go here.</CreateOrganisation.Wrapper>;
}

CreateOrganisation.Wrapper = styled.section``;
