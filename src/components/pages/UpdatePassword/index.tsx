import * as React from 'react';
import styled from '@emotion/styled';

export default function UpdatePassword(): React.ReactElement<{}> {
  return <UpdatePassword.Wrapper>Update Password will go here.</UpdatePassword.Wrapper>;
}

UpdatePassword.Wrapper = styled.section``;
